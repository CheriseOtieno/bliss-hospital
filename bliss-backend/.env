# ── SERVER SETTINGS ─────────────────────────────
PORT=8080
ENV=development

# ── DATABASE (Local PostgreSQL) ────────────────
# Format: postgres://DB_USER:DB_PASSWORD@DB_HOST:5432/DB_NAME
DATABASE_URL=postgres://postgres:ROBINH00D@127.0.0.1:5432/blissdb

# ── JWT SECRET ─────────────────────────────────
# Run: openssl rand -hex 32 to generate a strong secret
JWT_SECRET=3cf093c3ce0f4b9b55f7604ca177733cfe4bea52a5da3e6d80618a34f4c413bd

# ── OPTIONAL EMAIL (Gmail SMTP) ───────────────
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password